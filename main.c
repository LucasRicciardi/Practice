#include <stdio.h>

struct composite_state_t
{
    queue_t event_queue;    // Fila de eventos

};

int main()
{
    composite_state_t state_machine; // Instância de uma máquina de estados

    return 0;
}
